import React, { useState } from "react";

function TodoFilter() {
  const [filter, setFilter] = useState({
    createdDate: "today",
  });

  return (
    <>
      <div></div>
    </>
  );
}

export default TodoFilter;
